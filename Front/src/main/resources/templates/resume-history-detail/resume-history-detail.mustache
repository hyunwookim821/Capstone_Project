{{>layout/header}}
<div class="h-20 md:h-24"></div>

<main class="container mx-auto px-6 py-12">
    <div class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-800 mb-6">서류 피드백 상세</h1>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            {{resume.title}} 에 대한 AI 분석 결과입니다.
        </p>
    </div>

    <div class="max-w-6xl mx-auto space-y-10">
        <!-- 원본 및 수정된 내용 섹션 -->
        <div class="bg-white rounded-3xl p-8 shadow-lg fade-in">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">원본 및 맞춤법 교정</h2>
            <div class="grid md:grid-cols-2 gap-6">
                <div>
                    <h3 class="text-lg font-semibold text-gray-700 mb-3">원본 내용</h3>
                    <textarea class="w-full h-64 p-4 border-2 border-gray-200 rounded-lg bg-gray-50" readonly>{{resume.content}}</textarea>
                </div>
                <div>
                    <h3 class="text-lg font-semibold text-gray-700 mb-3">수정된 내용</h3>
                    <textarea class="w-full h-64 p-4 border-2 border-blue-300 rounded-lg bg-blue-50" readonly>{{resume.correctedContent}}</textarea>
                </div>
            </div>
        </div>

        <!-- AI 종합 피드백 섹션 -->
        <div class="bg-white rounded-3xl p-8 shadow-lg fade-in-delay">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">AI 종합 피드백</h2>
            <div id="feedback-container" class="prose lg:prose-xl max-w-none p-6 bg-gray-50 rounded-2xl min-h-[150px]">
                <!-- JavaScript에 의해 채워질 내용 -->
            </div>
            <div id="feedback-data" class="hidden">{{resume.aiFeedback}}</div>
        </div>

        <!-- 예상 면접 질문 섹션 -->
        <div class="bg-white rounded-3xl p-8 shadow-lg fade-in-delay-2">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">AI 예상 면접 질문</h2>
            <div class="bg-gray-100 rounded-xl p-4 mb-8 text-sm text-gray-700">
                <p><strong class="font-semibold">[공통]</strong>: 직무/인성과 관련된 일반적인 질문입니다.</p>
                <p><strong class="font-semibold">🌶️</strong>: 지원자의 역량과 경험을 깊이 파고드는 압박 질문입니다.</p>
            </div>
            <ul class="space-y-6">
                {{#resume.generatedQuestions}}
                <li class="flex items-start">
                    <div class="flex-shrink-0 w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center font-bold text-lg mr-4">Q</div>
                    <p class="text-gray-800 text-lg leading-relaxed">{{questionText}}</p>
                </li>
                {{/resume.generatedQuestions}}
            </ul>
            {{^resume.generatedQuestions}}
            <div class="text-center py-12">
                <p class="text-gray-500 text-lg">생성된 면접 질문이 없습니다.</p>
            </div>
            {{/resume.generatedQuestions}}

            <!-- 모의 면접 진행 버튼 -->
            <div class="text-center mt-12">
                <a href="/interview/start?resumeId={{resume.resumeId}}&use_existing_questions=true" class="bg-gradient-to-r from-blue-500 to-purple-500 text-white px-12 py-4 rounded-2xl text-lg font-semibold hover:from-blue-600 hover:to-purple-600 transition-all transform hover:scale-105 shadow-lg">
                    이 질문으로 AI 모의 면접 진행하기
                </a>
            </div>
        </div>
    </div>
</main>

<!-- marked.js CDN -->
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const feedbackDataEl = document.getElementById('feedback-data');
        const feedbackContainer = document.getElementById('feedback-container');

        if (feedbackDataEl && feedbackContainer) {
            const markdownText = feedbackDataEl.textContent;
            feedbackContainer.innerHTML = marked.parse(markdownText);
        }
    });
</script>

{{>layout/footer}}
